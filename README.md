# Tournesol

School project

# Usefull commands

```console
docker build --rm -t tournesol:latest .

docker run -d -p 8000:80 tournesol:latest

docker ps

docker exec -it name /bin/sh

docker stop
```
